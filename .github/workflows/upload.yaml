# totally not partially copied from anybody 👀👀 thx to @Fabulously-Optimized on GitHub
name: Build and upload to GitHub

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: github-actions
    permissions:
      contents: write
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Install Golang
        uses: actions/setup-go@v5
        with:
          go-version: 'stable'

      - name: Install packwiz
        run: go install github.com/packwiz/packwiz@latest
        
      - name: Export to packwiz
        run: |
          $(go env GOPATH)/bin/packwiz modrinth export -o Jurodedu-Neoforged-${{ github.ref_name }}.mrpack
      
      - name: Upload to Github Release
        uses: softprops/action-gh-release@v2
        with:
          files: Jurodedu-Neoforged-${{ github.ref_name }}.mrpack
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
